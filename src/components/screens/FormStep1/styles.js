import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  background1: {
    position: 'absolute',
    left: -150,
    top: -20,
    width: '200%',
    height: 500,
  },
});

export default styles;
