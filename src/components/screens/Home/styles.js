import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  background1: {
    position: 'absolute',
    left: -150,
    top: -20,
    width: '200%',
    height: 500,
  },
  background2: {
    position: 'absolute',
    left: 0,
    top: 430,
    width: '100%',
    height: '100%',
  },
});

export default styles;
